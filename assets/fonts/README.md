# Fonts

Self-contained fonts for Neuro Connect (no CDN required at runtime).

- `Outfit-Variable.ttf` — UI display / body
- `JetBrainsMono-Regular.ttf` — code blocks

These are downloaded by `scripts/fetch-fonts.ps1` / `scripts/fetch-fonts.sh` during setup.
